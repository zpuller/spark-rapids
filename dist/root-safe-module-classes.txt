# Module artifactIds whose classes are safe to promote to the dist jar root.
#
# The packager reads class paths from these module jars, then promotes the
# matching classes from the shaded aggregator output so relocated dependencies
# still resolve at runtime.
sql-plugin-format
